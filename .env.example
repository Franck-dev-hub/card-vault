# -----------------------------------------------------------------------------
# Card Vault - environment template
# Copy to .env (dev) / .env.prod / .env.preprod and adjust the values.
#   cp .env.example .env
# -----------------------------------------------------------------------------

# Project
PROJECT_NAME=cardvault
PROJECT_ENV=dev
PROJECT_DOMAIN=card-vault.fr
PROJECT_USER_ID=1000
PROJECT_GROUP_ID=1000
PROJECT_IMAGE_TAG=latest
DOCKER_REGISTRY=ghcr.io/franck-dev-hub

# Versions
PHP_VERSION=8.4
FRANKENPHP_VERSION=1.12.6
COMPOSER_VERSION=2.9
NODEJS_VERSION=22
PYTHON_VERSION=3.12
POSTGRES_VERSION=16
REDIS_VERSION=7
CADDY_VERSION=2.10

# PHP extensions
PHP_EXTENSIONS="intl opcache apcu pdo_pgsql redis sodium"
SESSION_LIFETIME=604800

# API
APP_SECRET=change-me
XDEBUG_MODE=debug

# Database
POSTGRES_DB=cardvault
POSTGRES_USER=cardvault
POSTGRES_PASSWORD=change-me

# Tools
PGADMIN_PASSWORD=change-me

# ML service
HF_TOKEN=

# CI / E2E
PLAYWRIGHT_VERSION=1.49.0
